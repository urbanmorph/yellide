# Screenshots needed

Two, both taken on a current build (the name must read **Yellide**, not "Yellide Probe"):

**warning.png**, the red "Installing will grant this extension access to everything on
your computer" dialog, with the Install Anyway button visible. This is the single most
important image on the site: it is where a non-technical install dies.

**installed.png**. Settings → Extensions → Yellide, showing the version number. Used to
explain that sideloaded extensions do not auto-update.

Crop to the dialog/panel, not the whole screen. PNG, 2x retina is fine.
