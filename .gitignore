# Dependencies
node_modules/
.pnpm-store/

# Secrets. The repo is public, so this list is load-bearing rather than tidy.
.env
.env.local
.env.*.local
# wrangler's local secret file, holds PEPPER
.dev.vars
.dev.vars.*
*.pem
*.key
.cloudflare
.wrangler/

# Build output
dist/
build/
.output/
.vercel/
.wrangler/
.svelte-kit/

# OS / editor
.DS_Store
*.log

# Tangential exploration / research / scratch — never committed (repo discipline)
supporting-docs/

# Local agent instructions. Kept out of the repo deliberately: it is working notes
# for whoever is driving Claude Code here, not documentation for contributors.
# Note it remains readable in history prior to the commit that untracked it.
CLAUDE.md

# Built bundle. The copy served by the website IS tracked — without it a fresh
# clone deploys a site whose download button 404s.
*.mcpb
!site/yellide.mcpb

# browser-automation scratch
.playwright-mcp/
